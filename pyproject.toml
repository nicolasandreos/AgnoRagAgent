[project]
name = "agnoasimov"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Nicolas", email = "nicolasandreose@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "agno[openai,os,pdf]>=2.9.0",
    "chonkie[semantic]>=1.7.0",
    "chromadb>=1.5.9",
    "pypdf>=6.16.1",
    "python-dotenv>=1.2.2",
    "streamlit>=1.62.0",
    "tavily-python>=0.7.27",
    "yfinance>=1.6.0",
]

[tool.pyright]
venvPath = "."
venv = ".venv"

[project.scripts]
agnoasimov = "agnoasimov:main"

[build-system]
requires = ["uv_build>=0.12.4,<0.13.0"]
build-backend = "uv_build"
